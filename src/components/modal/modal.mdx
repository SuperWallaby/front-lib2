import {JDmodal} from "./Modal";
import { Props } from "@storybook/addon-docs/blocks";

# Modal

Modal 컴포넌트는 기본적으로 `HTML Div`이 가진 Attribute들을 상속 받습니다.  
Hook `Use Modal`과 함께 사용하세요.

<Props of={JDmodal} />
