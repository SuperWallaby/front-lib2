import { Button } from "./Button";
import { Props } from "@storybook/addon-docs/blocks";

# Button


Button 컴포넌트는 기본적으로 `HTML Button`이 가진 Attribute들을 상속 받습니다.  
버튼은 누를떄마다 `User Tracking History`를 추가 시킵니다.
<Props of={Button} />
